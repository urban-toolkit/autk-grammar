{
  "name": "the-urban-grammar-monorepo",
  "private": true,
  "workspaces": [
    "grammar",
    "adapters/autk"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "install:all": "npm install --workspaces"
  }
}
